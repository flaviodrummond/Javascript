// Escreva um programa que compare duas strings e exiba "As strings são iguais" se as strings forem iguais, ou "As strings são diferentes" se forem diferentes.

const stringOne = "Eu tenho uma mente milionária"
const stringTwo = "Vou ser foda em Javascript"

if(stringOne === stringTwo) {
    console.log("As strings são iguais")
} else {
    console.log("As strings são diferentes")
}