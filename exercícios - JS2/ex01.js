// Escreva um programa que verifique se um número é par ou ímpar e exiba o resultado no console.

const number = 15

if(number % 2 === 0) {
    console.log(`${number} é número par`)
} else {
    console.log(`${number} é número impar`)
}