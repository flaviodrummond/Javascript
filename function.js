/* Funções / Functions 

As funções possibilita a gente ter um maior controle e um maior poder do que vamos fazer e quando. */


function printingOnScreen() { // Para criar uma função, usamos a palavra function e eu preciso dá um nome a minha função, abros os () e depois coloco a {} e dentro da chave, vai ser tudo que eu quero colocar dentro da função.
    console.log("Avançando cada dia mais com o DevClub") 
    // independente do que coloca dentro da função, ela só é executada, quando ela for chamada.

} 

printingOnScreen() // e para chamar uma função, coloco o nome dela e () 